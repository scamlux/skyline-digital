# 0002 — Отклонения стека от §2

**Статус:** OPEN — требует решения владельца · **Дата:** 2026-08-20

§2 ТЗ фиксирует стек как «не обсуждается». Ниже — накопившиеся отклонения кода от §2/§8. Эта запись их **фиксирует явно**, но НЕ узаконивает: до решения владельца каждое считается дефектом кода относительно ТЗ. Разбор — `../AUDIT.md`.

| # | Требование ТЗ | Факт в коде | Оценка | Что решить |
|---|---|---|---|---|
| A | AI: Anthropic Messages API, `claude-sonnet-4-6`, ключ только `ANTHROPIC_API_KEY` (§2/§8) | `openai` SDK, `gpt-4o-mini`, `OPENAI_API_KEY` | 🔴 нарушение «не обсуждается» | Мигрировать на `@anthropic-ai/sdk` **или** владелец официально принимает OpenAI и правит §2/§8 |
| B | 3D обязателен: R3F/drei, 3D-hero (§3) | 3D нет; анимации на CSS | 🔴 функция не реализована | Реализовать R3F-hero **или** понизить 3D до CSS в спеке |
| C | React Hook Form (§2) | формы вручную (`FormData`/`useState`) | 🟡 работает, но не по §2 | Принять ручной подход **или** внедрить RHF |
| D | `@supabase/ssr` (§2) | `@supabase/supabase-js` (service-role) | 🟡 блокирует Auth админки | Добавить `@supabase/ssr` при постройке админки |
| E | Пакетный менеджер pnpm; гейт `pnpm typecheck && lint && build && test` (§2/§10) | npm; скриптов `typecheck`/`test` нет | 🟡 гейт §10 не запускается | Перейти на pnpm + добавить скрипты **или** принять npm |
| F | Next.js 15 (§2) | Next.js 16.3.1 | 🟢 новее, App Router/RSC сохранены | Обновить §2 до 16 |
| G | Vitest + Playwright (§2/§10) | тестов нет | 🔴 денежный движок без тестов | Добавить хотя бы Vitest на движок цен |
| H | Проекты из БД, редактируемо (§5/§6) | `src/data/projects.ts` (static) | 🔴 нет админки/таблицы | Перевести на Supabase при постройке админки |

## Последствия отсутствия решения
Пока пункты A/B/G/H открыты, продукт не соответствует §2/§3/§5/§8/§10. Приоритеты — в `../../TODO.md` и `../AUDIT.md`.
